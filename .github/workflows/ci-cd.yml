name: Build, Deploy, and Test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v3

    # Step 2: Set up Ruby for Jekyll
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'
        bundler-cache: true

    # Step 3: Install dependencies and build the Jekyll site
    - name: Install dependencies
      run: |
        bundle install
        bundle exec jekyll build

    - name: Set up .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0'

    - name: Restore Selenium test dependencies
      run: dotnet restore SeleniumTests/SeleniumTests.csproj

    - name: Run Selenium Tests
      run: dotnet test SeleniumTests/SeleniumTests.csproj --logger "trx;LogFileName=TestResults.trx"

    - name: Install Allure CLI
      run: |
        curl -o allure.tgz -L https://github.com/allure-framework/allure2/releases/download/2.20.1/allure-2.20.1.tgz
        tar -xvzf allure.tgz
        sudo mv allure-2.20.1 /usr/local/allure
        sudo ln -s /usr/local/allure/bin/allure /usr/local/bin/allure

    - name: Generate Allure Report
      run: |
        mkdir -p ./_site/test-results
        allure generate SeleniumTests/TestResults -o ./_site/test-results

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./_site
